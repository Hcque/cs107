#include <iostream>
#include <map>
#include "definition.h"

using namespace std;

int main() {
    ifstream input;
    map<string, Definition> m;
    m["w"] = Definition(&input);
    for (auto x: m) {
        cout << x.first << endl;
    }
    return 0;


}
